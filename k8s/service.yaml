apiVersion: v1
kind: Service
metadata:
  name: fintrack-backend-service
  labels:
    app: fintrack-backend
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: fintrack-backend
